You are authoring a storyboard markdown file for an automated tutorial-video generator. Below is a COMPLETE example in the required format. Study its structure: a small YAML front-matter block, a `# Title` heading, a free-form brief, then one plain `## Scene Title (~Ns)` heading per scene with the scene's visual description written directly under it.

Produce a NEW storyboard explaining **{{NAME}}** (category: {{CATEGORY}}) from the TOGAF enterprise-architecture framework, in the SAME format and the same level of detail.

TOGAF topics are NOT all taught the same way. The teaching method best suited to THIS topic is **{{METHOD}}**. The worked EXAMPLE at the end of this prompt is a complete storyboard built with that same method — match its visual vocabulary, its teaching arc, and its depth, but adapt everything to {{NAME}} (do NOT copy the example's topic).

Teaching-method catalog (use the one named above):
- adm_cycle — a process / phase topic. Draw the ADM as a clockwise ring of Phases A–H around a central Requirements Management hub, with the Preliminary phase drawn OUTSIDE the ring feeding an arrow into Phase A (Architecture Vision) and Phase H looping back to Phase A to close the ring; put the phase in focus, then walk its inputs -> steps -> outputs and one worked pass of the running example.
- technique — an applied technique. Lead with the technique's signature TOOL (a matrix or grid, a scored list, a heat-map), show how to read it, work one concrete case, then say which ADM phases use it.
- concept — a foundational idea. Define the term(s) as titled cards, show how they RELATE with labelled arrows, contrast any commonly-confused pair, then ground it in a short example.
- structure — a framework, repository, continuum, or reference model. Show its STRUCTURE / taxonomy (layered bands, or a generic -> specific spectrum, or a metamodel of related boxes), then place concrete example assets into it.
- governance — a capability / governance topic. Show the governance PROCESS (a small cycle) and STRUCTURE (a board with its controls), use a scale or ladder for a compliance or maturity level, then a worked review.

Hard requirements:
- front-matter has exactly: `title: {{TITLE}}`, `subject: togaf`, `language: both`, `length: 2-3 minutes`, `preparation_profile: togaf`.
- the `# ` title heading reads exactly `# {{NAME}}`.
- the brief (the prose right after the `# ` title, before the first `## ` scene) must state that TOGAF is a methodology/framework taught with clean labelled diagrams — boxes, the ADM cycle, matrices, layered bands, spectrums, and relationship maps — never as source code and never with an invented "official TOGAF icon" (TOGAF has no standard icon set). The brief should also name the main visual the {{METHOD}} method uses for this topic, and state that every TOGAF term is explained in plain words for a viewer who has never heard it.
- include a single `# Preparation` section (a level-1 `#` heading, placed AFTER the brief and BEFORE the first `## ` scene) instructing that, before any scene is drawn, the canonical structure and terminology of {{NAME}} must be verified against authoritative TOGAF sources (The Open Group's TOGAF Standard) rather than recalled from memory — so the diagram matches the official method, not an invented one. Name the specific facts to confirm for this topic. Whenever the ADM cycle is involved, the block must require confirming that the ring is Phases A–H only, that the Preliminary phase sits OUTSIDE the ring feeding an arrow into Phase A (Architecture Vision) and is NOT a box on the ring, that Requirements Management is the central hub, and that Phase H loops back to Phase A. This is a `#` block, NOT a `## ` scene: it has no `(~Ns)` duration and is never rendered.
- EXPLAIN THE JARGON: TOGAF is full of technical terms ordinary people have never heard. The FIRST time any TOGAF term appears — in the brief or in a scene (Enterprise Architecture, Architecture Vision, Building Block, Baseline/Target Architecture, Gap, Viewpoint, Enterprise Continuum, Repository, Governance, Compliance, and so on) — gloss it in a few plain words, ideally with a quick everyday comparison (e.g. "a Building Block is a reusable part, like a LEGO brick"). Keep the real term so the viewer learns it, but always pair it with its plain meaning. Never use a TOGAF term cold.
- 6 to 8 scenes; each is a plain `## Human Title (~Ns)` heading (the `(~Ns)` is that scene's length in seconds) with the description right underneath. Do NOT write `## Scene:`, `**file:**`, `**fallback_duration:**`, `**class:**`, or `### description` — those are derived automatically.
- the per-scene seconds must sum to between 120 and 180.
- the FIRST (intro) scene must present the topic name as the LARGE CENTERED title (not only in the small top bar) — it reads exactly `{{NAME}}`. A short subtitle may sit directly beneath it.
- this is an ENTERPRISE-ARCHITECTURE METHODOLOGY tutorial, NOT program code: build every scene from labelled boxes, cycles, matrices, bands, and relationship arrows, never source code.
- whenever the ADM cycle appears, draw it the canonical way — Phases A through H clockwise on a ring with Requirements Management as the central hub joined to every phase, the Preliminary phase OUTSIDE the ring with one arrow into Phase A (Architecture Vision), and Phase H (Architecture Change Management) looping back to Phase A to close the ring — and keep those phase names and that order consistent across scenes.
- follow the teaching arc of the {{METHOD}} method (see the catalog and the worked example): locate the topic within TOGAF -> its purpose -> its key parts shown with that method's main visual, each with a short example -> the deliverables, outputs, or neighbouring phases it connects to -> ONE worked example in a concrete case -> a conclusion.
- use ONE concrete running example from EVERYDAY LIFE that ordinary people know (for example a small neighbourhood coffee shop, a family bakery, a school canteen, or a corner grocery starting to take orders online) — not a large abstract corporation. Do NOT give the business a made-up brand or company name — refer to it generically ("the coffee shop", "the bakery"). Name the example's own elements in plain English, and keep the TOGAF vocabulary in English too (Architecture Vision, Business Architecture, Architecture Roadmap, Building Block, and so on) — but always paired with its plain meaning per the jargon rule above. The English version should be entirely in English, including the examples.
- descriptions ONLY (no narration text, no Manim/Python code blocks). Use semantic colors DANGER/OK/HIGHLIGHT/PRIMARY/ACCENT as the example does.
- avoid orientation words (left/right/above/below) in any text meant to be shown or spoken.

Output ONLY the raw markdown file content. No code fences, no commentary before or after.

===== EXAMPLE STORYBOARD (format reference) =====
{{EXEMPLAR}}
===== END EXAMPLE =====
